# temperature_monitoring_device
A high-temperature (up to 300 °C) ESP32 monitoring device that reads temperature data and publishes it to an MQTT server. The device supports Wi-Fi setup through built-in Access Point mode, stores credentials, and uses a NeoPixel LED to indicate connection status.
